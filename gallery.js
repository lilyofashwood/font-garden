(function(){'use strict';const $=id=>document.getElementById(id);let pending=0;
function render(){const source=$('source').value;if(source.length>20000){$('status').textContent='Source exceeds 20,000 UTF-16 code units.';return;}
const query=$('filter').value.toLowerCase(),styles=FontGarden.list().filter(s=>(s.name+' '+s.family+' '+s.role).toLowerCase().includes(query));$('gallery').replaceChildren();$('status').textContent=styles.length+' registers';
for(const s of styles){const card=document.createElement('article'),name=document.createElement('h2'),note=document.createElement('p'),sample=document.createElement('div'),copy=document.createElement('button');name.textContent=s.name;note.className='hint';note.textContent=s.family+' · '+s.role;sample.className='specimen';sample.textContent=FontGarden.apply(s.id,source);copy.textContent='copy '+s.name;copy.onclick=async()=>{try{await navigator.clipboard.writeText(sample.textContent);$('status').textContent=s.name+' copied';}catch{const range=document.createRange();range.selectNodeContents(sample);const selection=window.getSelection();selection.removeAllRanges();selection.addRange(range);$('status').textContent='Text selected. Use your normal Copy shortcut.';}};card.append(name,note,sample,copy);$('gallery').append(card);}}
for(const id of ['source','filter'])$(id).addEventListener('input',()=>{cancelAnimationFrame(pending);pending=requestAnimationFrame(render);});render();})();
